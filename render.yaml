services:
  - type: web
    name: opencord-server
    runtime: node
    region: oregon
    plan: free

    buildCommand: npm install && npm run build -w server
    startCommand: node server/dist/index.js

    envVars:
      - key: NODE_ENV
        value: production

      - key: PORT
        value: "10000"

      - key: TRUST_PROXY
        value: "1"

      - key: COOKIE_SECURE
        value: "true"

      - key: PUBLIC_URL
        value: "https://open-cord-remix-client-k5l6.vercel.app"

      - key: ALLOWED_ORIGINS
        value: "https://open-cord-remix-client-k5l6.vercel.app"
